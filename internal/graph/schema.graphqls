# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type User {
  id: Int!
  name: String!
  email: String!
  followers: [User!]!
}

type Token {
  token: String!
}

type Query {
  logIn(login: String!, password: String!): Token! 
  listFollowers(userId: Int!): [User!]!
  user(id: Int!): User!
  users(id: [Int!]!): [User!]!
}

type Mutation {
  signUp(login: String!, email: String!, password: String!): Token! 
}
